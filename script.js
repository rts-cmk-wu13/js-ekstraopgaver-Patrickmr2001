// Lav dine funktioner her:

// assignmentA: Tjekker om to værdier er ens i både værdi og type
function assignmentA(a, b) {
    return a === b;
}

// assignmentB: Returnerer de tre sidste tegn i en given string
function assignmentB(string) {
    return string.slice(-3);
}

// assignmentC: Tjekker om et tal er lige
function assignmentC(number) {
    return number % 2 === 0;
}

// assignmentD: Finder det mindste tal i et array, som er større end 0
function assignmentD(array) {
    return Math.min(...array.filter(num => num > 0));
}

// assignmentE: Kan skælne imellem ord med b og B
function assignmentE(array) {
    return string ("bil", "banan", "Bord", "hest", "abe", "Kaffe");
    string = string.toLowerCase();

}